# Timeshift Installation Commands

# Install repository keys
sudo add-apt-repository -y ppa:teejee2008/timeshift

# Update system repositories
sudo apt-get update

# Install Timeshift Application
sudo apt-get install timeshift
